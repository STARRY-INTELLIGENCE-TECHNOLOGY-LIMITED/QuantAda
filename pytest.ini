[pytest]
testpaths = tests

# 工作区可能残留中断运行产生的受保护 pytest 缓存临时目录，正常收集时
# 不要递归进入这些目录。
norecursedirs =
    .git
    .venv
    .pytest_cache
    .pytest_cache_*
    pytest-cache-files-*
    __pycache__

# 将 pytest 的小型元数据缓存放在项目内新的可写目录中，避免使用残留的
# 受保护 .pytest_cache 目录。
cache_dir = .pytest_cache_quantada
